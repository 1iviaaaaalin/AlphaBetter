# AlphaBetter｜浏览器部署说明

这份版本已经把前端和 Python 后端合并成一个网站。

部署完成以后：

> 打开一个网址 → 上传 PDB → 开始分析 → 直接看 3D 结构。

日常使用不需要 VS Code、Python、Node.js，也不需要打开终端。

## 第 1 步：解压项目

下载 `AlphaBetter_云端部署版.zip`，解压后得到 `AlphaBetter_cloud` 文件夹。

不要删除 `data` 文件夹，也不要只上传其中几个文件。

## 第 2 步：用浏览器上传到 GitHub

1. 打开 GitHub。
2. 登录自己的账号。
3. 新建一个 Repository，例如：`AlphaBetter`。
4. 进入这个仓库，选择上传文件。
5. 把 **AlphaBetter_cloud 文件夹里面的全部文件和文件夹**上传进去。
6. 确认仓库根目录直接看到这些内容：

```text
Dockerfile
app.py
utils.py
calc_stability.py
requirements.txt
render.yaml
data/
frontend/
```

特别注意：不要出现这种多套一层的结构：

```text
AlphaBetter/
  AlphaBetter_cloud/
    Dockerfile
```

正确的是：

```text
AlphaBetter/
  Dockerfile
  app.py
  ...
```

## 第 3 步：打开 Render

1. 注册/登录 Render。
2. 选择 `New` → `Web Service`。
3. 连接刚刚的 GitHub 仓库。
4. 选择 `AlphaBetter` 仓库。
5. Runtime / Language 选择 `Docker`。
6. Dockerfile 使用根目录的 `Dockerfile`。
7. 计划可以先选择 Free。
8. 创建并部署。

本项目已经准备好 `render.yaml`、Dockerfile 和健康检查接口，不需要自己写启动命令。

## 第 4 步：等待部署

Render 会自动：

```text
读取 Dockerfile
↓
安装 Python 依赖
↓
安装前端依赖
↓
构建 React 网页
↓
启动 FastAPI
↓
生成公网网址
```

部署成功以后会得到一个类似：

```text
https://你的服务名.onrender.com
```

以后这个网址就是 AlphaBetter 的正式入口。

## 第 5 步：测试

打开网址：

1. 选择 `test.pdb`
2. 点击「开始分析」
3. 等待分析完成
4. 页面出现 3D 蛋白质结构
5. 检查颜色是否出现红、黄、绿
6. 点击「下载彩色 PDB」测试下载

## 本版本已经修复的关键问题

### 1. 不再使用 localhost

前端不再请求：

```text
http://127.0.0.1:8000
```

而是请求当前网站自己的：

```text
/api/upload
```

所以部署到云端后，浏览器会自动访问云端后端。

### 2. 二级结构识别

PDB 有 `HELIX` / `SHEET` 注释时优先使用这些结构标签；没有时才使用项目原来的 USSA 风格规则作为后备方案。

对于项目测试的 `test.pdb`，已经验证能够得到正、负稳定性分数，而不是全部 0。

### 3. 3D 颜色

网页不再直接使用 NGL 默认的 B-factor 最小值/最大值映射。

现在明确以 0 为中心：

```text
负值  ←  红色
 0    ←  黄色
正值  →  绿色
```

并且正负两侧采用渐变，而不是只有三种硬切颜色。

### 4. 云端运行

每次上传使用独立的临时目录，不会因为两个用户同时上传文件而互相覆盖计算结果。

### 5. 路径和 Python 环境

稳定性脚本使用当前 Python 解释器，不再硬编码 `python3`；项目路径也全部使用绝对路径计算，避免云端工作目录不同导致找不到文件。
